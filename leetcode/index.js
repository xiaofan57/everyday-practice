const { record, dump } = require("./scripts/run")

const today = {
  id: "206",
  title: "反转链表",
  link: "reverse-linked-lis",
  difficulty: "easy",
  tags: ["链表"],
}

// console.log(dump(/*id*/))

record(today)

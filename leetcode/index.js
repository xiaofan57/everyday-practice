const { record, dump } = require("./scripts/run")

const today = {
  id: "19",
  title: "删除链表的倒数第N个结点",
  link: "remove-nth-node-from-end-of-list",
  difficulty: "medium",
  tags: ["链表"],
}

// console.log(dump(/*id*/))

record(today)
